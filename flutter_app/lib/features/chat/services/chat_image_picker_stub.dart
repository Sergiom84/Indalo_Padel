import 'package:image_picker/image_picker.dart';

Future<String?> pickChatImageAsDataUrlImpl(ImageSource source) {
  throw UnsupportedError(
    'La seleccion de imagenes no esta disponible en esta plataforma.',
  );
}
